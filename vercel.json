{
    "version": 2,
    "functions": {
      "api/categories": {
        "handler": "server.js",
        "runtime": "@vercel/node"
      },
      "api/getall": {
        "handler": "server.js",
        "runtime": "@vercel/node"
      }
    },
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/"
      }
    ]
  }
  
  